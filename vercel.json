{
  "version": 2,
  "builds": [
    {
      "src": "apps/web-app/package.json",
      "use": "@vercel/next"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/$1" },
    { "src": "/", "dest": "/index.html" }  // 루트 경로를 명시적으로 지정
  ]
}
